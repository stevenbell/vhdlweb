Write a testbench to check that the module `mux41` actually performs correctly as a 4:1 multiplexer. (Hint: it doesn't.)
<br/>
<br/>
Your code should print the word "failed" if the DUT fails any of your tests.  We'll do some automated testing later to ensure that a correct module passes your tests.

